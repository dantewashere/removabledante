{
  "name": "removabledante",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass": "sass --style=compressed src/styles/main.scss public/styles/main.css",
    "watch:eleventy": "eleventy --serve",
    "watch:sass": "npm run sass -- --watch",
    "start": "npm-run-all sass --parallel watch:*",
    "build": "cp -r public/* ./",
    "eleventy": "npm-run-all sass --parallel"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^0.12.1",
    "sass": "^1.34.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
